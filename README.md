# zippy

`zippy` is a sample application that has no chart or CI. It's used to demonstrate Konstruct's automatic application build and deploy system.

Register zippy as a new application with Konstruct and deliver to your environments.
